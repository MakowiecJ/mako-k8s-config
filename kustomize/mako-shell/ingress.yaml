apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mako-shell
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mako-shell
                port:
                  name: http
